{
  "schemaVersion": 1,
  "id": "${mod.id}-gametest",
  "version": "${mod.version}",
  "name": "${mod.name} Game Test",
  "icon": "${mod.id}.png",
  "environment": "*",
  "entrypoints": {
    "fabric-gametest": ["io.github.leawind.gitparcel.platform.gametest.GitParcelGameTestFabric"],
    "fabric-client-gametest": []
  }
}
