{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": ["${author}"],
  "contact": {
    "homepage": "${home_url}",
    "sources": "${source_url}",
    "issues": "${issues_url}"
  },
  "license": "${license}",
  "icon": "${logo_file}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "${group}.common.platform.fabric.Entrypoint"
    ],
    "client": [
      "${group}.client.platform.fabric.ClientEntrypoint"
    ],
    "server": [
      "${group}.server.platform.fabric.ServerEntrypoint"
    ]
  },
  "mixins": [
    "${id}.mixins.json",
    "${id}.fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": "*",
    "minecraft": ">=${minecraftVersion}",
    "modernui": ">=${modernui_version}",
    "system_storage_lib": ">=${system_storage_lib_version}"
  }
}
