{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": ["${author}"],
  "contact": {
    "homepage": "${home_url}",
    "sources": "${source_url}",
    "issues": "${issues_url}"
  },
  "license": "${license}",
  "icon": "${logo_file}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "${group}.core.mc.platform.fabric.Entrypoint"
    ],
    "client": [
      "${group}.client.mc.platform.fabric.ClientEntrypoint"
    ],
    "server": [
      "${group}.server.mc.platform.fabric.ServerEntrypoint"
    ]
  },
  "mixins": [
    "${id}.mixins.json",
    "${id}.fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": "*",
    "minecraft": ">=${minecraftVersion}",
    "modernui": ">=${modernui_version}",
    "system_storage_lib": ">=${system_storage_lib_version}"
  }
}
