/*? if neoforge {*/
/*package io.github.leawind.gitparcel.server.platform.neoforge;

import io.github.leawind.gitparcel.common.api.GitParcel;
import io.github.leawind.gitparcel.server.minecraft.logic.ModServerEntrypoint;
import net.neoforged.api.distmarker.Dist;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.common.Mod;

@Mod(value = GitParcel.MOD_ID, dist = Dist.DEDICATED_SERVER)
public class ServerEntrypoint {
  public ServerEntrypoint(IEventBus modBus) {
    ModServerEntrypoint.initialize();
    ServerEntrypoint.initialize();
  }

  public static void initialize() {}
}
*//*?}*/
