{
  "schemaVersion": 1,
  "id": "${id}-gametest",
  "version": "${version}",
  "name": "${name} Game Test",
  "icon": "${logo_file}",
  "environment": "*",
  "entrypoints": {
    "fabric-gametest": ["io.github.leawind.gitparcel.gametest.GitParcelGameTestFabric"],
    "fabric-client-gametest": []
  }
}
